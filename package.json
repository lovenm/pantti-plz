{
  "scripts": {
    "dev": "bun index.html",
    "build": "rimraf ./out/* && bun build index.html --outdir ./out --minify",
    "format": "biome format --write ."
  },
  "dependencies": {
    "@zxing/library": "^0.21.3"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.5-nightly.81fdedb",
    "rimraf": "^6.0.1"
  }
}
